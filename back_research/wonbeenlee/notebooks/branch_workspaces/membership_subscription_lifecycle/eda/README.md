# Membership Subscription Lifecycle EDA Workspace

이 디렉토리는 Branch B용 EDA 작업 공간이다.

우선 대상 데이터셋:

- `saas_subscription_churn`
- `kkbox_churn_prediction_challenge`
- `ibm_telco_customer_churn`
- `streaming_subscription_churn_model`
- `x5`

핵심 질문:

- join/tenure/renewal/expiry 구조를 어떻게 panel로 만들 것인가
- membership churn과 일반 activity drop을 어떻게 구분할 것인가
- lock-in score와 연결할 핵심 lifecycle feature는 무엇인가
